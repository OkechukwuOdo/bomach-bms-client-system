import 'package:flutter/material.dart';

class ProjectAssociate extends StatelessWidget {
  const ProjectAssociate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}