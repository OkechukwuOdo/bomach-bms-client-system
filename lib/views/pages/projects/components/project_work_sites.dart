import 'package:flutter/material.dart';

class ProjectWorkSite extends StatelessWidget {
  const ProjectWorkSite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}