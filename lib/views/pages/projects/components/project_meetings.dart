import 'package:flutter/material.dart';

class ProjectMeetings extends StatelessWidget {
  const ProjectMeetings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}